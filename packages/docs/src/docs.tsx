import { Application } from '@morten-olsen/gallery';
import widgets from './widgets';

const docsApp: Application = {
  name: 'Docs',
  identifier: 'docs',
  screens: [],
  widgets,
  searchers: [],
};

export default docsApp;
